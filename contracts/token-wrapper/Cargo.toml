[package]
name = "recurra-token-wrapper"
version = "0.1.0"
edition = "2021"
description = "Recurra Token Wrapper — Subscription-aware token operations"
license = "MIT"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
