[package]
name = "recurra-multi-sig-governance"
version = "0.1.0"
edition = "2021"
description = "Recurra Multi-Signature Admin Governance contract"
license = "MIT"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
