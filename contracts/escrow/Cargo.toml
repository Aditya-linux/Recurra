[package]
name = "recurra-escrow"
version = "0.1.0"
edition = "2021"
description = "Recurra Escrow & Dispute — Dispute resolution with fund escrow"
license = "MIT"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
