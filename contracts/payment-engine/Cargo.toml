[package]
name = "recurra-payment-engine"
version = "0.1.0"
edition = "2021"
description = "Recurra Recurring Payment Engine — Core billing contract"
license = "MIT"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
