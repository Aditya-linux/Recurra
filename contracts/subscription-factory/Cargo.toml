[package]
name = "recurra-subscription-factory"
version = "0.1.0"
edition = "2021"
description = "Recurra Subscription Factory — Plan template store for merchants"
license = "MIT"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
